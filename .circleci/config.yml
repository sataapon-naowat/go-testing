version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python:3.8

    steps:
      - run:
          name: Start Docker Compose
          command: |
            docker-compose up -d

workflows:
  continuous-integration:
    jobs:
      - build
