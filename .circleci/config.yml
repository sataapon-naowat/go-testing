version: 2.1
jobs:
  build:
    machine:
      image: ubuntu-2004:202010-01

    steps:
      - run:
          name: echo Hello Sataapon
          command: |
            echo "Hello Sataapon"

workflows:
  continuous-integration:
    jobs:
      - build
